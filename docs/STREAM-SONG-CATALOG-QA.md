# 曲一覧の検証・収集引き継ぎ（PR #191）

この文書は PR #191 の作業記録と再現手順です。ルールの正本は `AGENTS.md`、配信メモの手順は `docs/LIVE-STREAM-RECAP.md`、楽曲出典の記録は `docs/CONTENT-OPS.md`。この文書はそれらを変更しません。

## 担当を戻すとき

- 対象は `ackey1007fw-coder/mily-fan-site` の PR #191、既存 branch `jipi/stream-song-catalog-20260906`。
- PR の最新 head・Conversation・未コミット変更を確認してから担当を切り替える。同じ branch / ファイルを並行編集しない。別 branch で同じ機能を再実装しない。
- オーナーの明示指示により Draft を維持し、Ready 化・merge・main への直接 push はしない。
- 開始 head は `e17ea24`。2026-09-07 の参照 main は `03346fb`（#192）。曲データの正本は merged #185 / #190 を含む各配信の `songs`。検索一覧用の第二の手入力マスターを作らない。
- `docs/AI_HANDOFF.md` / `docs/DECISION_LOG.md` / `docs/AI_PROJECT_MEMORY_SKILL.md` は確認時の main に存在しない。Project Memory は新設していない。
- 2026-09-07（3回目の引き継ぎ・Claude Code）: 競合なしで main `d42dad0` を branch へ merge（rebase / force-push なし）。`src/ActivitiesPage.tsx` の末尾改行を既存スタイルへ揃えた。アプリ実装・各回 `songs`・allowlist・画像は未変更。

## ブラウザ検証

`scripts/stream-song-catalog.browser.mjs` は実際の `pnpm build` 出力を Vite preview で起動し、Playwright で操作する。HTML やコンポーネントを模倣したテスト用の別実装ではない。

`.github/workflows/ci.yml` は従来の install / typecheck / test / build / guard の後にブラウザ検証を実行する。ブラウザ用 Playwright 1.61.1 は runner の一時領域に固定版をインストールし、アプリの package.json / lockfile は変更しない。テスト失敗を無視せず CI を失敗させる。

| 環境 | viewport | 確認対象 |
| --- | --- | --- |
| Chromium desktop | 1280 × 900 | PC 2列表示 |
| Chromium mobile | 390 × 844 | スマートフォン相当 1列表示 |
| WebKit mobile | 390 × 844 | 別エンジンのスマートフォン相当表示 |
| Chromium narrow | 320 × 740 | 狭幅での折り返し・横 overflow |

各環境で次を操作する（入口・版注記の検証を含む10グループ、合計40グループ）。

1. 初期件数・新しい順・横 overflow の検査と一覧のスクリーンショット。
2. 曲名・アーティスト文字列・全角英字と複数語の検索。
3. アーティスト絞り込み、曲名順と新しい順の切り替え。
4. 0件表示と「検索条件をクリア」による検索語・アーティストの解除。
5. 原曲・参考伴奏のリンクが既存データと一致し、新規タブ属性と説明を持つこと。
6. キーボードで配信一覧を展開し、配信へ移動。移動先を閉じて同一 hash を再クリックしたときも再展開すること。
7. 新しいページ読み込みで配信への直リンクが対象カードを開くこと。
8. RADIO には曲一覧を出さず、実行中のページ例外がないこと。

### 証跡の読み方

CI の `song-catalog-browser-<run id>` artifact に `report.json` と4環境の PNG を保存する（保持7日）。失敗時は実行済みグループと failure PNG を記録する。report の `head` が PR head、`commit` が実際に checkout した commit（pull_request 実行時は合成 merge commit）である。古い head の成功を現 head の成功として扱わない。

最初の成功は [CI #545](https://github.com/ackey1007fw-coder/mily-fan-site/actions/runs/34066830434)、head `2cb5b9f`。後続 commit の検証結果は PR Conversation を参照する。

これは **CI 内のビルド成果を実ブラウザで操作した結果**であり、次の確認とは別である。

- 保護された Vercel Preview そのものの操作。専用 fetch は共有 URL の生成に失敗した。
- iPhone / Android 実機での操作。mobile は viewport / touch のエミュレーションである。
- YouTube 外部プレイヤーの再生可否、地域・年齢・ログイン制限。
- 元動画の歌唱確認やプレイリスト全件調査。

CI のブラウザは外部通信を遮断する。非公開アーカイブや SNS へアクセスせず、外部フォントも取得しないため、スクリーンショットはこの条件での表示証跡である。

### 手元で再実行

Node / pnpm の版は package.json と CI を優先する。以下は POSIX 環境向け。成果物を public/ や git 管理下へ書き出さない。

```sh
pnpm install --frozen-lockfile
pnpm typecheck
pnpm test
pnpm build
pnpm guard
TOOLS_DIR="$(mktemp -d)"
EVIDENCE_DIR="$(mktemp -d)"
npm install --prefix "$TOOLS_DIR" --no-package-lock --ignore-scripts --no-audit --no-fund playwright@1.61.1
node "$TOOLS_DIR/node_modules/playwright/cli.js" install chromium webkit
PLAYWRIGHT_MODULE_ROOT="$TOOLS_DIR/node_modules" \
SONG_CATALOG_ARTIFACT_DIR="$EVIDENCE_DIR" \
PR_HEAD_SHA="$(git rev-parse HEAD)" \
node --disable-warning=ExperimentalWarning --experimental-strip-types scripts/stream-song-catalog.browser.mjs
```

Linux のブラウザ依存ライブラリが不足する場合は CI の `install --with-deps` 手順を参照する。既存の差分チェックも実行する。

## 収集状況（2026-09-08 再開）

- 指定プレイリストはブラウザで開け、66本の一覧と限定公開の表示を確認できた。以前の「URLが途中で切れている可能性」は解消。URLの再共有は不要。
- 54本の自動字幕を取得し、歌唱関連語とその前後から候補を調べた。12本は字幕取得不能。取得・キーワード探索は全編聴取でも歌唱の網羅確認でもない。
- 歌唱前後の案内と複数節の歌声字幕を照合して、8/25朝「ロマンスの神様」、8/18夜「ぼよよん行進曲」の2曲を追加。合計は5配信6曲。各回データの `songs` が引き続き正本。
- 「ぼよよん行進曲」のリンクは原曲歌手も参加する公式企画動画。原盤音源と誤認させないよう、一覧と配信メモの両方で版を明記した。
- 短い口ずさみ・曲名への言及・過去の歌唱を振り返った発言は、この追加に含めていない。
- 元録画参照・字幕・候補区間の台帳は公開repo外に保存。字幕のない回はローカル音声認識で探索中。自動字幕や音声認識で不明な区間は未確認のまま残す。
- Draft PR #191 の既存branchを継続。Draft解除・merge・本番反映はしていない。

## 過去の収集状況（2026-09-07 時点）

**全件収集は未完了。元プレイリストの内容・有効性・公開範囲・総本数を取得できていない。** 取得失敗を「プレイリストが空」「歌唱なし」「URLが無効」と読み替えない。

- 既存 main 由来の3配信4曲のみ。今回新たに元動画を視聴した件数は0、新たに追加した歌唱曲は0。
- `streamRecaps` の掲載済み12回はサイトの配信メモ数であり、指定プレイリストの本数でも調査完了数でもない。
- 既存4曲の根拠・確認方法は #185 / #190 と CONTENT-OPS を参照する。提供記録・歌唱区間の自動認識・公式情報の照合を、全編の手動聴取済みとは書かない。
- 今回は保存資料2件を再読した。9/5昼のレポートでは録画内0:22:40頃が「朝の歌を振り返る」場面と明記されており、昼の新規歌唱として追加しない。9/4夜の配信メモからも、新規歌唱を肯定できる根拠は増えていない。いずれも全編の歌唱なしを確認したという意味ではない。
- 原曲・カラオケの既存8 URL、allowlist、配信データ、画像、予定、NEWS、投票導線は今回の引き継ぎで変更していない。

## 収集ログ（バッチ別チェックポイント）

プレイリストは **10本程度ずつのバッチ**で処理し、バッチごとに「調査済み動画・確認できた曲・未確認事項・コード / データへ反映済みか」を
この表へ1行で残してから次へ進む。一度に全件を処理しない（長時間処理の途中停止で成果が失われた前例があるため）。
動画 URL / ID・非公開参照・全文文字起こし・歌詞はこの表へ書かない。本数と結果だけを残す。

| 日付 | 担当 | 対象 | 調査済み動画 | 新規確認曲 | 未確認 | コード / データ反映 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-06 | 一時引き継ぎ（1回目） | 既存 main の songs | 0（新規視聴なし） | 0 | プレイリスト取得不能 | 一覧機能のみ。曲データ変更なし |
| 2026-09-07 | 一時引き継ぎ（2回目） | 保存資料の再読 2件 | 0（新規視聴なし） | 0 | 残り11件は所在のみ | ブラウザ検証と本文書のみ。曲データ変更なし |
| 2026-09-07 | 一時引き継ぎ（3回目・Claude Code） | 指定プレイリスト バッチ1 | 0（取得不能） | 0 | 下記「取得試行の記録」 | 本文書と末尾改行のみ。曲データ変更なし |
| 2026-09-08 | 再開 | 一覧1〜10 | 自動字幕9本、全編手動聴取なし | 0 | 字幕なし1本・口ずさみ候補 | 既存曲を維持 |
| 2026-09-08 | 再開 | 一覧11〜20 | 自動字幕10本、全編手動聴取なし | 0 | 口ずさみ候補 | 新規曲なし |
| 2026-09-08 | 再開 | 一覧21〜30 | 自動字幕9本、歌唱前後を部分照合 | 1 | 字幕なし1本・会話中の候補 | 8/25朝の歌唱メモを追加 |
| 2026-09-08 | 再開 | 一覧31〜40 | 自動字幕8本、歌唱前後を部分照合 | 1 | 字幕なし2本 | 8/18夜の歌唱メモを追加 |
| 2026-09-08 | 再開 | 一覧41〜50 | 自動字幕6本、全編手動聴取なし | 0 | 字幕なし4本・口ずさみ候補 | 新規曲なし |
| 2026-09-08 | 再開 | 一覧51〜60 | 自動字幕9本、全編手動聴取なし | 0 | 字幕なし1本・部分歌唱候補 | 新規曲なし |
| 2026-09-08 | 再開 | 一覧61〜66 | 自動字幕3本、全編手動聴取なし | 0 | 字幕なし3本 | 既存曲を維持 |

### 取得試行の記録（2026-09-07・3回目。その環境での過去記録）

以下は当時の取得制約の記録。9/8再開時にはブラウザで66本を確認しており、URLの再共有を待つ状態ではない。

- 指定プレイリスト URL への HTTPS 接続は、作業環境の egress policy により `www.youtube.com:443` への CONNECT が 403 で拒否された。
  ページ取得ツール（WebFetch）も同一ドメインを `EGRESS_BLOCKED` として拒否した。ポリシー拒否のため回避・再試行はしていない。
- Web 検索でプレイリスト ID を照会したが、一致する結果はなかった。
- 指定 URL の `list=` パラメータは一般的な YouTube プレイリスト ID より短く、共有時に切り詰められている可能性がある。
  推測で補完せず、オーナーへ完全な URL（または動画一覧）の再共有を確認する。
- repo 内（`media/original/` を含む）に元動画・文字起こし等の調査素材は存在しない。
- 以上により、このセッションで視聴・確認した動画は 0 本、新規に確定した曲は 0 曲。既存 main 由来の **3配信4曲**（Mela! / メメント・モリ / 生まれてはじめて / 愛をこめて花束を）から変更なし。
  複数回歌唱が確認できた曲は現時点で 0 曲。

### 未確認として記録するもの（公開データへは追加しない）

いずれも「歌唱を確認できた」根拠がないため `songs` へ入れていない。原動画または提供資料で歌唱区間・曲名・原曲アーティストが確定した場合だけ追加する。

| 配信 | 手がかり | 状態 |
| --- | --- | --- |
| 2026-09-02 夜 | timeline 1:06:00頃「歌のプレイリスト練習中」 | 話題のみ。曲名・歌唱の有無とも未確認 |
| 2026-09-04 夜 | 曲名を使った言葉遊び（CONTENT-OPS 点検記録） | 歌唱ではない。追加しない |
| 2026-09-05 朝 | 「ケセラセラ」を選曲候補として言及 | 候補のみ。歌唱未確認 |
| 2026-09-05 昼 | 0:22:40頃 朝の歌の振り返り | 歌唱なしと確認済み。追加しない |
| 2026-09-02〜09-04 の各回 | 録画全編の再聴取なし | 歌唱の有無を断定しない |
| 未確定の歌唱候補 | 9/8に66本の一覧と候補を調査 | 短い口ずさみ・認識困難な区間は引き続き未掲載 |

### 次のバッチで確定した曲の反映先（再実装しない）

1. 該当回の `src/data/streamRecap*.ts` の `songs` に `{ title, artist, timestamp, youtubeUrl }`（任意で `karaoke`）を追加。配信メモがない回は既存 StreamRecap 型で最小の確認済み本文を先に作る。
2. 新しい原曲 URL は `scripts/approved-song-links.mjs` の allowlist へ同時追加し、`docs/CONTENT-OPS.md` に確認日と照合方法（アーティスト / レーベル公式チャンネルであること）を記録する。非公式転載・歌詞動画は採用しない。
3. 同じ曲を複数回歌った場合も、各回の `songs` にそのまま追加すればよい。`src/lib/streamSongCatalog.ts` が曲名＋アーティストで自動的にまとめ、歌唱履歴を配信ごとに保持する。第二の曲マスターを作らない。
4. `pnpm typecheck` / `pnpm test` / `pnpm build` / `pnpm guard` を通し、上の表へ1行追加して commit する。

## 次の収集手順

1. オーナー提供の元プレイリストを開ける環境で一覧を取得し、全ページ・非公開/削除/取得不能の項目を含む収録範囲を確定する。指定 URL の ID を推測補完しない。開けない場合は、その事実を残してオーナーへ URL の再共有または動画一覧を確認する。
2. オーナー管理の非公開作業領域で、動画ごとに調査台帳を作る。最低限「元の並び番号・非公開参照・配信日とその根拠・閲覧可能性・見た/聴いた区間・全文/部分/未調査・歌唱候補・確定曲・録画内開始時刻・未解決理由」を分ける。再生リストへの所属が未確認の保存資料は別枠にする。
3. 「資料の存在だけ確認」「レポートのみ確認」「自動文字起こしのみ確認」「映像/音声の一部確認」「全編確認」を区別する。タイトルへの言及、選曲相談、前回の振り返り、楽器演奏を歌唱に変換しない。歌唱なしは十分に確認した範囲だけで判断する。
4. 配信日・曲名・原曲アーティスト/版・歌唱開始の目安が確定したものだけ既存の各回 `songs` に追加する。まだ配信メモのない動画は、既存 StreamRecap 型と手順に沿って最小の確認済み本文を作り、空欄を推測で埋めない。
5. 新しい原曲 URL はアーティスト/レーベルの個別公式動画で照合し、オーナー確認と既存運用に従って出典台帳・厳密な allowlist・回帰テストを同時更新する。参考伴奏と本人の歌唱を混同しない。
6. 全体品質ゲート・ブラウザ検証を再実行し、確認できた動画数/未調査数/取得不能数/追加曲数を別々に報告する。全件数が不明なら網羅率を作らない。

元動画の URL / ID、認証情報、全文文字起こし、歌詞、視聴者情報は、この文書・public PR・CI の引数/ログ/artifact に置かない。公開側には集計と確認方法だけを残す。

### 9/8 再開時のローカル検証（6曲・5配信時点）

- typecheck / test（1,452件）/ build / guard 成功。
- Chromium PC・390px mobileは検索・絞り込み・リンク・版注記・配信移動を含む9グループが成功。390px一覧を目視確認。
- Windows WebKitは表示・検索・版注記が成功したが、1回目はTemporal.Duration例外、再試行は同一hash再展開待ちで失敗。成功とは記録しない。ログにスタックを残すよう改善し、current headのLinux CIでも確認する。
- 元録画の音声解析・外部YouTube再生はUIテストの検証範囲外。


## 9/8 再開後の収集結果

- 一覧66本を確認。54本は自動字幕から歌唱候補と前後の文脈を探索し、字幕を取得できなかった12本はローカルで全音声の区間検出を実行して候補区間を音声認識した。
- これは全編の手動視聴・聴取ではなく、自動認識を用いた候補探索と部分照合である。検出漏れや不明瞭な区間は残り、66本の全歌唱を網羅したとは表示しない。短い口ずさみ・曲名だけの会話・認識困難な候補は掲載を保留。
- 保存データは **13曲・13配信・18回の歌唱記録**。元の4曲・3配信・4回から、9曲・10配信・14回を追加。同曲の繰り返しも各回に保存し、一覧だけで重複をまとめる。
- 新規曲は「ロマンスの神様」「ぼよよん行進曲」「アイドル」「可愛くてごめん」「SWEET MEMORIES」「元彼女のみなさまへ」「かわいいだけじゃだめですか？」「超最強」「好きすぎて滅！」。既存曲「愛をこめて花束を」「生まれてはじめて」の過去履歴も追加。
- 各曲の照合方法・原曲の出典は CONTENT-OPS.md に記録。原盤以外の公式企画動画・歌唱版は一覧と各回の両方に注記。
- 66本の参照、認識区間、確認結果はgitignore対象のローカル調査台帳へ保存。公開コード・PRには元録画参照・歌詞・認識本文を含めない。
- 6曲時点のhead `fb48677` は [Linux CI](https://github.com/ackey1007fw-coder/mily-fan-site/actions/runs/34161307952) の全品質ゲートと4環境36グループのブラウザ検証が成功。Windows WebKitの失敗とは区別する。13曲へ拡張後の最新headはPR checksで確認する。

### 再開時の最初の操作

PR #191のheadとDraft状態・未コミット変更を確認し、ローカルの非公開調査台帳にある未確定区間から照合を続ける。再生リスト再取得や一覧機能の再実装からやり直さない。歌唱を確定できた場合のみ各回の `songs` と出典台帳を更新する。

## 9/8 配信コーナーの入口を強化

- LIVE STREAMページの冒頭、紹介文の直後に「♪ みりぃの歌リスト」の大きなカードを追加。「歌リストを見る」から一覧へ直接移動する。件数は各回のsongsから計算する。
- Activitiesの配信カードにも「みりぃの歌リストを見る」を追加し、詳細ページの一覧へ直接移動できる。
- 4環境で冒頭の入口・Activitiesからの移動先・横overflowを確認し、入口の画像も保存する。
- main #202の「366日」歌唱メモを統合したため、現時点の一覧は14曲・14配信・19歌唱。前節の13曲は前回保存時点の記録。Draft #191を維持し、本番公開はしていない。

## LIVE SONG CLIPS の追加ルール（2026-09-12）

- 歌唱クリップを公開できる回だけ、既存 `songs[]` の同じ歌唱記録へ任意の `clip` を追加する。別の曲マスター・動画マスターを作らない。
- `clip` は公開MP4、実フレームposter、実寸、短い抜粋の秒数、録画内の開始位置だけを保持する。録画原本や私的な許可確認情報は保持しない。
- `/activities/live/clips/` は `streamRecaps` から `clip` のある歌唱記録だけを導出する。曲リスト側の検索・原曲URL・歌唱履歴を複製しない。
- クリップは短い配信記録として扱い、フル曲・歌詞テロップを既定にしない。公開可否と楽曲・伴奏の権利確認は別のゲートとして扱う。
- 過去回は、歌唱と対応録画を内容で照合し、掲載承認を満たしたものだけ段階的に追加する。件数合わせで推測・別回流用をしない。
