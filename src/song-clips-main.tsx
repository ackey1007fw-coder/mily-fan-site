import React from "react";
import ReactDOM from "react-dom/client";
import SongClipsPage from "./SongClipsPage";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SongClipsPage />
  </React.StrictMode>,
);
